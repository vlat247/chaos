import { CaptureInterface } from "@/components/capture-interface";

export default function Home() {
  return (
    <main className="dark min-h-screen bg-background flex items-center justify-center">
      <CaptureInterface />
    </main>
  );
}
